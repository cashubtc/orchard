/** Number of days metric samples are retained before deletion */
export const METRICS_RETENTION_DAYS = 90;

/** Age in days after which minute-granularity samples are downsampled to hourly */
export const METRICS_DOWNSAMPLE_AFTER_DAYS = 7;
